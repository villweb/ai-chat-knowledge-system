# 执行日志

本文档记录每次实际执行的内容，最新记录放在最上方。

记录字段：

- 日期
- 执行目标
- 修改文件
- 完成项
- 未完成项
- 偏离 spec 的地方
- 下一步

## 2026-06-23 P6 知识库体验

执行目标：

- 先阅读 P5 验收报告，确认 P5 三条验收标准已通过，可进入 P6。
- 完成 P6 知识库体验：搜索、筛选、重复合并、每日沉淀日历、Markdown 导出、Obsidian 索引、备份和恢复。

修改文件：

- `app/core/knowledge-library.ts`
- `app/core/index.ts`
- `app/desktop/main.cjs`
- `app/desktop/preload.cjs`
- `app/desktop/renderer/main.tsx`
- `app/desktop/renderer/styles.css`
- `scripts/knowledge-library.ts`
- `tests/p6-knowledge-library.test.ts`
- `spec/产品化开发步骤.md`
- `spec/执行日志.md`

完成项：

- 新增 `knowledge-library` Core 模块，提供知识搜索、来源/类型/项目/标签/状态筛选、facet 聚合、重复组识别和每日沉淀日历。
- 新增 Markdown 导出，输出到 `data/exports/markdown-<timestamp>/`，并生成 Obsidian 可识别的 wikilink 索引。
- 新增 `knowledge/_index.md` 生成入口，提升现有 `knowledge/` 目录在 Obsidian 中的浏览体验。
- 新增本地备份与恢复最近备份能力，覆盖 `knowledge`、`data/runtime`、`data/daily_runs` 和 `logs`。
- 新增 `scripts/knowledge-library.ts`，供桌面主进程调用视图、导出、Obsidian 索引、备份和恢复。
- 桌面主进程新增 `knowledge:view`、`knowledge:export-markdown`、`knowledge:ensure-obsidian`、`knowledge:backup`、`knowledge:restore-latest` IPC。
- preload 暴露同名 `desktopApi` 方法。
- 桌面端新增“知识库”页，包含搜索框、来源/类型/项目/标签/状态筛选、知识列表、重复合并、沉淀日历、导出、Obsidian 索引、备份和恢复入口。
- 已将 `spec/产品化开发步骤.md` 中 P6-01 到 P6-10 勾选完成，并把近期下一步推进到 P7。

验收记录：

- 已阅读 `spec/执行日志.md` 顶部 P5 验收报告：P5 三条验收标准全部满足，可进入 P6。
- `npm run typecheck` 通过。
- `npm run sqlite:rebuild` 通过，用于修复当前 Node ABI 下的 `better-sqlite3` 原生模块。
- `npm test` 通过，23 个测试全部通过，其中 P6 专项 4 个用例覆盖筛选、重复组、日历、导出、Obsidian 索引、备份和恢复。
- `npm run desktop:build` 通过，产物写入 `dist/desktop/`。
- `node -c app/desktop/main.cjs` 通过。

未完成项：

- P6 的恢复入口首版只支持恢复最近一次本地备份，不做多备份选择、增量备份或云端备份。
- 重复识别首版基于标题、类型和正文前段的确定性归组，不引入语义相似度模型。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P7 隐私、安全和合规。

## 2026-06-23 P5 验收

执行目标：

- 对 P5「每日自动化」按 `spec/产品化开发步骤.md` 的三条验收标准做一次独立验收。
- 静态检查（typecheck/test/语法）+ 端到端实跑三条验收标准 + 核对 P5-01 到 P5-09 任务。

验收结果：P5 三条验收标准全部满足，可进入 P6。

自动化检查：

- `npm run sqlite:rebuild` 通过（恢复系统 Node ABI）。
- `npm run typecheck` 通过（EXIT=0）。
- `npm test` 通过，19 个测试全部通过，其中 P5 专项 7 个用例（默认保守设置、空闲等待、确认等待、当日完成跳过、当日跳过不再提示、失败重试次数与延迟、运行历史聚合）全绿。
- `node -c app/desktop/main.cjs` 语法检查通过。

端到端验收（隔离临时 vault `/tmp/p5-accept-*`，未改动真实数据）：

- 验收标准①（用户可设置每日自动运行）：`scripts/daily-automation.ts save-settings` 写入 `enabled/run_time_local/retry_count/only_when_idle`，`get-state` 读回一致；决策引擎联动 settings+history+idle，输出 `not_due`（当前真实时间未到 22:30）。通过。
- 验收标准②（自动运行不重复导入同一批记录）：同一批 Codex 示例连续导入两次，SQLite `normalized_records` 仍为 2 条、`knowledge/inbox/` 仍为 2 张卡片，证明稳定 `record_id` + upsert 去重生效。通过。
- 验收标准③（失败后能定位失败来源和原因）：注入损坏 JSON 后单文件失败计入 `failures[]`（带 `raw_path` 与具体 `error_message`），整批不中断；`list-history` 聚合输出 `status=failed | source=codex | err="Expected property name..."`，来源与原因可定位。通过。

P5-01 到 P5-09 任务核对：

- P5-01/02 定时设置与运行时间：`daily-automation-settings.json` 持久化 + `run_time_local` HH:mm 校验。
- P5-03 仅空闲时运行：`only_when_idle` + `idle_threshold_seconds`，主进程用 `powerMonitor.getSystemIdleTime()` 判断，决策 `waiting_idle`。
- P5-04 运行前确认：`require_confirmation` → `pending_confirmation`，主进程写 `pendingAutomationRun` 并发通知。
- P5-05 运行后通知：`notify_on_complete` + `showNotification`。
- P5-06 失败重试次数：`retry_count`/`retry_delay_minutes` → `retry_later`/`retry_now`/`retry_exhausted`。
- P5-07 跳过已处理记录：upsert 去重 + 提炼侧 `findKnowledgeAtom`/标题去重。
- P5-08 指定日期重跑：`automation:rerun-date` + `--run-date`，`allow_rerun` 绕过当日已完成跳过。
- P5-09 运行历史查看：`listDailyRunHistory` 聚合 `data/daily_runs/*.json`。

结论：

- P5 通过验收，可进入 P6 知识库体验。

下一步：

- 进入 P6 知识库体验，优先做搜索、按来源/类型/项目/标签筛选、每日沉淀日历和导出。

## 2026-06-23 P5 每日自动化

执行目标：

- 实现应用运行时每日自动化：Electron 打开时由主进程定时检查，到点后按配置触发每日沉淀。
- 默认策略保持保守：关闭状态、22:30、仅空闲时运行、运行前确认、完成通知、失败重试 1 次。

修改文件：

- `app/core/daily-automation.ts`
- `app/core/index.ts`
- `app/core/manual-import-normalization-runner.ts`
- `app/core/ai-knowledge-extraction.ts`
- `app/desktop/main.cjs`
- `app/desktop/preload.cjs`
- `app/desktop/renderer/main.tsx`
- `app/desktop/renderer/styles.css`
- `scripts/daily-automation.ts`
- `scripts/run-manual-import-normalization.ts`
- `scripts/extract-knowledge-atoms.ts`
- `tests/p5-automation.test.ts`
- `spec/产品化开发步骤.md`
- `spec/执行日志.md`

完成项：

- 新增每日自动化设置，持久化到 `data/runtime/daily-automation-settings.json`。
- 新增自动化决策纯函数，覆盖到点判断、同日已完成跳过、空闲等待、确认等待、失败重试和重试耗尽。
- 新增运行历史聚合，读取 `data/daily_runs/*.json` 并展示日期、状态、来源、生成数量和错误摘要。
- 新增 `scripts/daily-automation.ts`，用于主进程读取状态、保存设置和列出历史。
- 桌面主进程每 30 秒检查一次配置，使用 `powerMonitor.getSystemIdleTime()` 判断空闲。
- 到点需要确认时写入 `pendingAutomationRun`，通过 IPC 暴露给 UI，并发送系统通知。
- 自动运行、确认运行和指定日期重跑复用现有每日沉淀流水线。
- 失败后按配置重试，超过次数后写入失败运行记录。
- 跳过本次会写入 `cancelled` 运行历史，避免当天反复弹出确认。
- preload 暴露 `automation:get-state`、`automation:save-settings`、`automation:confirm-run`、`automation:skip-run`、`automation:rerun-date`、`automation:list-history` 对应方法。
- Run 页面新增自动化设置、待确认运行卡片、指定日期重跑入口和运行历史列表。
- 已将 `spec/产品化开发步骤.md` 中 P5-01 到 P5-09 勾选完成，并把近期下一步推进到 P6。

验收记录：

- `node -c app/desktop/main.cjs` 通过。
- `node --import tsx scripts/daily-automation.ts get-state --vault-root . --idle-seconds 999` 通过。
- `npm run sqlite:rebuild` 通过。
- `npm run typecheck` 通过。
- `npm test` 通过，19 个测试全部通过。
- `npm run desktop:build` 通过，产物写入 `dist/desktop/`。
- 静态检查 Run 页面桌面和移动布局：新增区域使用 `minmax(0, ...)`、`min-width: 0` 和移动端单列断点；未发现明显横向溢出风险。

未完成项：

- P5 首版只保证应用打开时自动运行，不做系统级后台常驻、开机启动、OS 任务计划、自动网页登录或公司项目接入。
- 当前本机预览工具未提供可直接操作的浏览器动作能力，未完成真实浏览器点击截图；已用构建、类型检查、测试和静态布局检查替代。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P6 知识库体验，优先补搜索、筛选、每日沉淀日历和导出体验。

## 2026-06-23 P4 验收

执行目标：

- 对 P4 首批连接器按 `spec/产品化开发步骤.md` 的三条验收标准实跑验收。

验收记录：

- `npm run typecheck` 通过（EXIT=0）。
- `npm test` 通过，12 个测试全部通过，其中 P4 专项 3 个用例（注册表/Cursor 导入/DeepSeek 导入）全绿。
- 验收标准①（首批三来源至少一种稳定导入方式）：临时 vault 实跑，Codex、Cursor、DeepSeek 各导入 1 文件、标准化 1 条、失败 0。
- 验收标准②（每个连接器说明读了什么/没读什么）：`scripts/list-source-connectors.ts` 输出 5 个连接器的 `reads`/`does_not_read`/`permission_scope`/`failure_help`，均完整可读。
- 验收标准③（失败时给出明确原因）：预留连接器 `createSourceConnector("doubao")` 抛 `豆包 connector is reserved...`；损坏 JSON 不中断整批，单文件失败计入 `failures[]` 并带明确 `error_message`。
- 10 项任务核对：P4-01 注册表、P4-02 启用/禁用（含预留连接器拦截与禁用当前源时自动切换）、P4-03/04 Codex、P4-05/06 Cursor、P4-07 DeepSeek、P4-08/09 豆包与 Workbuddy 预留、P4-10 格式与权限说明，全部满足。

结论：

- P4 通过验收，可进入 P5（每日自动化）。

下一步：

- 进入 P5 每日自动化。

## 2026-06-23 修复 P3 验收问题并完成 P4 首批连接器

执行目标：

- 先修复 P3 验收记录中指出的 `better-sqlite3` 原生 ABI 冲突。
- 直接进入 P4，完成 Codex、Cursor、DeepSeek 首批连接器，并为豆包和 Workbuddy 保留配置入口。

修改文件：

- `app/connectors/source-connector.ts`
- `app/connectors/manual-import-connector.ts`
- `app/connectors/source-registry.ts`
- `app/connectors/index.ts`
- `app/core/manual-import-normalization-runner.ts`
- `app/desktop/main.cjs`
- `app/desktop/preload.cjs`
- `app/desktop/renderer/main.tsx`
- `app/desktop/renderer/styles.css`
- `scripts/list-source-connectors.ts`
- `tests/p4-connectors.test.ts`
- `spec/产品化开发步骤.md`
- `spec/来源格式说明.md`
- `spec/执行日志.md`

完成项：

- P3 验收问题已修：桌面端派生 TS 脚本时改用系统 Node（`AI_KB_NODE_BINARY` / `npm_node_execpath` / `node`），不再用 Electron 二进制和 `ELECTRON_RUN_AS_NODE`，避免 Electron ABI 与 Node ABI 来回 rebuild。
- 新增连接器注册表：记录来源状态、默认启用、支持格式、导入目录、读取范围、不读取范围、识别规则和失败提示。
- Codex、Cursor、DeepSeek 标记为 P4 可用；豆包和 Workbuddy 标记为预留，不参与导入。
- Core 导入流程改为通过连接器注册表创建连接器。
- 桌面来源页支持查看连接器权限说明，并可启用/停用可用连接器。
- 设置页默认来源只允许选择已启用且可用的连接器。
- 新增 `scripts/list-source-connectors.ts`，便于 UI 和验收读取连接器注册信息。
- 新增 P4 测试，覆盖注册表、预留连接器阻断、Cursor 本地记录导入、DeepSeek 网页导出导入。
- 已将 `spec/产品化开发步骤.md` 中 P4-01 到 P4-10 勾选完成。

验收记录：

- `npm run sqlite:rebuild` 通过，当前原生模块已恢复为系统 Node ABI。
- `npm run typecheck` 通过。
- `npm test` 通过，12 个测试全部通过。
- `npm run desktop:build` 通过。
- `node --import tsx scripts/list-source-connectors.ts --enabled codex,cursor,deepseek` 通过，能输出五个连接器的权限和启用状态。

未完成项：

- 豆包和 Workbuddy 仍为预留连接器，不读取数据。
- 自动定时运行留到 P5；打包、签名、自动更新留到 P8/P9。

偏离 spec 的地方：

- 无。

下一步：

- 可进入 P5 每日自动化；如需继续加固 P4，可补桌面端 Electron 实机点击验收。

## 2026-06-23 P3 验收

执行目标：

- 按用户要求对 P3「桌面端最小可用版本」做一次独立验收。
- 静态检查（typecheck/test/build）+ 端到端跑通核心链路 + 核对三条验收标准。

验收结果：P3 三条验收标准全部满足，但发现一个 better-sqlite3 原生 ABI 冲突需修复。

自动化检查：

- `npm run typecheck` 通过。
- `npm test` 首次失败：better-sqlite3 报 `NODE_MODULE_VERSION 131 vs 127`（原生模块按 Electron ABI 构建，node 跑不起来）。执行 `npm run sqlite:rebuild` 后 9/9 通过。
- `npm run desktop:build` 通过，产物写入 `dist/desktop/`。

端到端验收（隔离临时 vault，未改动真实数据）：

- 导入+标准化：2 文件 → 2 标准化记录 → 2 原子。通过。
- AI 提炼（fixture）：生成 2 候选，front matter 含来源与证据。通过。
- 来源可见：每个原子均带 `source_app` 与 `source_raw_paths`。通过（验收标准 2）。
- 编辑后保存：改标题/正文/标签 → 落盘到 `knowledge/approved/`，磁盘内容核对一致。通过（验收标准 3）。
- 批准/拒绝/合并：分别落到 approved/rejected/merged 目录，inbox 同步移除。通过（P3-10）。
- 完整链路（导入→提炼→确认→写入）跑通。通过（验收标准 1）。
- API Key 安全：`main.cjs` 仅写入 `process.env`，不落盘/日志/导出。通过（P3-03）。

遗留问题（建议进 P4 前修）：

- 原生 ABI 冲突：桌面运行时用 `process.execPath`（Electron 二进制）+ `ELECTRON_RUN_AS_NODE=1` 派生 tsx 脚本，脚本里的 better-sqlite3 需按 Electron ABI 构建；而 `postinstall`/`sqlite:rebuild` 按系统 node ABI 构建，二者无法被同一份二进制同时满足。结果：`npm test` 与桌面端「导入/每日运行」（依赖 SQLite）不能同时可用，需要来回 rebuild。原日志「`npm test` 通过」与「`desktop:build` 通过」无法在单次 rebuild 下同时为真。建议：引入 `electron-rebuild` 并在 `desktop:dev/build` 前自动重建，或改用系统 node 派生脚本，或在文档中明确 rebuild 步骤。
- 此次端到端验证在系统 node 下完成，证明业务逻辑正确；桌面 Electron 运行时的 SQLite 链路因上述 ABI 问题尚未在本机实跑确认。

下一步：

- 修复 better-sqlite3 ABI 冲突后再进 P4 首批连接器。

## 2026-06-23 P3 桌面端最小可用版本

执行目标：

- 进入 P3，完成普通用户无需命令行即可运行每日沉淀的最小桌面端。
- 保持 P1/P2 核心能力不改动业务语义，通过 Electron UI 调用既有核心脚本。

修改文件：

- `app/core/index.ts`
- `app/core/knowledge-review.ts`
- `app/desktop/main.cjs`
- `app/desktop/preload.cjs`
- `app/desktop/renderer/main.tsx`
- `app/desktop/renderer/styles.css`
- `app/desktop/renderer/vite-env.d.ts`
- `scripts/extract-knowledge-atoms.ts`
- `scripts/review-knowledge-atom.ts`
- `index.html`
- `vite.config.ts`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `spec/产品化开发步骤.md`
- `spec/执行日志.md`

完成项：

- 新增 Electron 主进程和 preload，提供知识库选择、文件导入、每日运行、知识列表、知识更新、日志读取和会话配置 IPC。
- 新增 React/Vite 桌面端 UI，覆盖首次引导、来源管理、手动导入、每日运行、运行进度、待确认列表、知识详情、批准/拒绝/合并/编辑、设置页和日志入口。
- 新增 `knowledge-review` 核心模块和 `scripts/review-knowledge-atom.ts`，让 UI 通过 Core Engine 统一修改知识原子 Markdown 状态，不直接绕过存储边界。
- AI Key 只保存到当前进程环境变量，不写入磁盘、日志或导出文件。
- `scripts/extract-knowledge-atoms.ts` 支持 `--vault-root`，桌面端可以对用户选择的知识库运行 P2 提炼。
- 补齐 `desktop:dev`、`desktop:build`、`desktop:preview` 脚本和 Vite/React TypeScript 配置。
- 已将 `spec/产品化开发步骤.md` 中 P3-01 到 P3-12 勾选完成。

验收记录：

- `npm run typecheck` 通过。
- `npm test` 通过，9 个测试全部通过。
- `npm run desktop:build` 通过，产物写入 `dist/desktop/`。
- `npm run desktop:preview` 已启动并用本地浏览器检查桌面宽度：页面非空白，核心布局存在，无 console error，无零尺寸控件。
- 移动宽度 390x844 检查通过：无横向溢出、无越界控件、无 console error。

未完成项：

- 尚未实现跨平台安装包、签名、自动更新和商业授权；这些仍留在 P8/P9。
- Cursor、DeepSeek、豆包、Workbuddy 的稳定连接器仍留在 P4。

偏离 spec 的地方：

- 无。

下一步：

- 可进入 P4 首批连接器，优先做连接器注册机制和 Codex/Cursor/DeepSeek 的稳定导入入口。

## 2026-06-23 P2 验收复跑

执行目标：

- 按用户要求对 P2「AI 提炼和质量控制」做一次独立验收。
- 实跑 `spec/产品化开发步骤.md` 中 P2 全部验收标准，确认是否可进入 P3。

修改文件：

- `spec/执行日志.md`

验收结果：P2 通过，8 条验收标准全部满足。

- AI 输出符合知识原子结构：`validateDraft` 校验 + 输出合法 `KnowledgeAtom` Markdown。通过。
- 候选内容进入 `pending` 状态：生成卡片 front matter `review_status: pending`。通过。
- 每条候选有来源和证据：`source_record_ids`、`source_raw_paths`、`evidence` 齐全。通过。
- 无法判断的内容不进正式库：候选写入 `knowledge/inbox/`，不写 `approved/`。通过。
- 未显式授权不能触发生成：去掉 `--allow-ai` 直接抛 `AI extraction requires explicit user authorization`。通过。
- 未经授权不得把原始对话发给云端：授权门禁 + fixture provider 不联网，openai-compatible provider 需显式配置 env。通过。
- 敏感内容默认阻断：`prepareRecordForAI` 过滤非 personal 及公司、客户等业务风险记录。通过。
- `npm run p2:extract-candidates` 在 fixture provider 下完成本地验收：先 `p1:codex-import` 导入 2 条记录，再跑 P2 生成 2 条 pending 候选并写入每日摘要。通过。

复跑命令记录：

- `npm run typecheck` 通过。
- `npm test` 通过，4 个测试全部通过。
- `npm run p1:codex-import` 通过，生成 2 条标准化记录。
- `npm run p2:extract-candidates` 通过，生成 2 条 pending 候选。

验收发现（不阻断放行）：

- `better-sqlite3` 在当前 Node v22.19.0 首跑时报 ABI 不匹配（NODE_MODULE_VERSION 131 vs 127），需先执行 `npm rebuild better-sqlite3`。`postinstall` 已配自动 rebuild，但切换 Node 版本仍会复现，已在当前环境手动重建修复。
- 验收时 P2 自动化测试散落在 `tests/p1-core.test.ts`（仅覆盖授权、脱敏、去重），缺独立测试文件与业务风险阻断、低价值过滤、草稿校验、每日摘要覆盖。已在本轮补齐，见下条验收后续。

验收后续（同日补测）：

- 新增 `tests/p2-extraction.test.ts`，并将 P2 用例从 `tests/p1-core.test.ts` 迁出，P1 文件回归只测 P1。
- P2 测试覆盖：未授权拒绝、脱敏与非个人/业务风险记录阻断、fixture 候选生成与 pending+归档来源、每日摘要文件与计数一致、低价值草稿与引用不存在记录的过滤（注入自定义 generator）、二次运行去重与合并建议计数。
- `npm run typecheck` 通过；`npm test` 通过，9 个测试全部通过（P1 三项 + P2 六项）。

未完成项：

- 真实云端模型小样本验证仍待进行（需配置 openai-compatible provider）。

偏离 spec 的地方：

- 无。

下一步：

- 可进入 P3 桌面端最小可用版本；进入前建议补 P2 回归测试并用真实模型跑一轮小样本人工抽检。

## 2026-06-23 P1 回归修复与 P2 AI 提炼闭环

执行目标：

- 阅读 `spec/项目评审报告.md` 第 8 节和 `spec/监督问题记录.md` 最新回归监督记录。
- 修复进入 P2 前的 P1 回归问题。
- 直接进入 P2，完成最小 AI 提炼和质量控制闭环。

修改文件：

- `app/connectors/manual-import-connector.ts`
- `app/core/ai-knowledge-extraction.ts`
- `app/core/index.ts`
- `app/core/normalize-manual-import.ts`
- `app/core/rebuild-local-indexes.ts`
- `app/storage/local-raw-archive.ts`
- `package.json`
- `prompts/p2-knowledge-atom-extraction.yaml`
- `prompts/p2-privacy-classification.yaml`
- `prompts/p2-daily-summary.yaml`
- `scripts/extract-knowledge-atoms.ts`
- `tests/p1-core.test.ts`
- `spec/产品化开发步骤.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 修复重建 ID 漂移：稳定 `record_id` 不再依赖 `raw_path`，TXT 会话 ID 改为内容哈希。
- 原始归档新增 `.meta.json` 元数据，重建时恢复原始导入路径，并保持 `raw_archive_path` 独立承载归档路径。
- Markdown 导入 front matter 改用 `yaml` 解析，读写策略统一。
- `ManualImportConnector` 来源识别收敛为构造传入的 `source_app`。
- 新增 P2 prompt 契约：知识原子提炼、隐私分类、每日摘要。
- 新增 `extractKnowledgeAtoms`，实现授权门禁、AI 输入裁剪、敏感遮蔽、默认阻断、结构化输出校验、低价值过滤、重复识别、合并建议计数、pending 写入和每日摘要。
- 新增 fixture provider 用于本地可重复验收，并预留 OpenAI-compatible provider。
- 新增 `npm run p2:extract-candidates`。
- 测试新增 P2 授权、脱敏、候选生成和重复识别覆盖。

验证结果：

- `npm run typecheck` 通过。
- `npm test` 通过，4 个测试全部通过。
- `npm run p1:codex-import` 通过。
- `npm run p1:rebuild-indexes` 通过。
- `npm run p2:extract-candidates` 通过，生成 pending 候选并写入每日摘要。

未完成项：

- P2 目前本地验收使用 fixture provider；真实云端模型需要配置 `AI_KB_OPENAI_API_KEY`、`AI_KB_OPENAI_BASE_URL` 和 `AI_KB_OPENAI_MODEL` 后使用 openai-compatible provider。
- 桌面 UI、定时任务、多来源稳定导入仍属于 P3 以后。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P3 前，可先用真实模型跑一轮 P2 小样本，人工检查候选质量。

## 2026-06-23 P1 完整收口

执行目标：

- 按用户要求将 P1 完整开发完毕后再汇报。
- 处理项目评审报告中仍阻断 P1 放行的问题：测试、归档语义、checksum 持久化、隐私默认值、StorageProvider 聚合、路径工具复用和索引重建入口。

修改文件：

- `.gitignore`
- `package.json`
- `tsconfig.json`
- `app/connectors/manual-import-connector.ts`
- `app/core/core-engine.ts`
- `app/core/index.ts`
- `app/core/manual-import-normalization-runner.ts`
- `app/core/normalize-manual-import.ts`
- `app/core/rebuild-local-indexes.ts`
- `app/schemas/normalized-record.ts`
- `app/services/local-run-logger.ts`
- `app/storage/index.ts`
- `app/storage/knowledge-atom-markdown-writer.ts`
- `app/storage/local-raw-archive.ts`
- `app/storage/local-storage-provider.ts`
- `app/storage/paths.ts`
- `app/storage/sqlite-normalized-record-store.ts`
- `app/storage/storage-provider.ts`
- `app/storage/workspace.ts`
- `raw/archive/.gitkeep`
- `raw/imports/codex/示例记录.md`
- `raw/imports/codex/示例记录.json`
- `scripts/rebuild-local-indexes.ts`
- `tests/p1-core.test.ts`
- `spec/产品化开发步骤.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 原始记录归档改为写入 `raw/archive/<source_app>/<checksum>.<ext>`，不再覆盖 `raw/imports/` 源文件。
- `NormalizedRecord` 新增 `raw_archive_path` 和 `raw_checksum`，SQLite 持久化这两个字段，并记录 schema migration。
- `KnowledgeAtom.source_raw_paths` 改为指向独立归档副本。
- 未声明敏感度的 JSON、Markdown、TXT 默认按 `private` 处理，`can_enter_personal_kb=false`；示例文件显式声明 `sensitivity: personal` 用于 P1 验收。
- 新增 `LocalStorageProvider implements StorageProvider`，聚合目录确认、归档、SQLite 标准化记录、DailyRun、Markdown 写入和知识原子索引。
- `runManualImportNormalization` 改为通过 `LocalStorageProvider` 写入，并生成 `DailyRun` 文件。
- 抽出 `app/storage/paths.ts`，统一路径越界校验和 vault-relative path 转换。
- 新增 `rebuildLocalIndexes` 和 `npm run p1:rebuild-indexes`，可从 `raw/archive/` 重建标准化记录，并从 `knowledge/` Markdown 重建知识原子索引。
- 新增 `npm test` 和 `tests/p1-core.test.ts`，覆盖隐私默认阻断、导入闭环、幂等写卡、独立归档、checksum、DailyRun、索引重建、日志脱敏和安全文件名。

验证结果：

- `npm run typecheck` 通过。
- `npm test` 通过，3 个测试全部通过。
- `npm run p1:codex-import` 通过，处理 2 个 Codex 示例文件，生成 2 条标准化记录和 2 张待确认卡片，失败 0。
- `npm run p1:rebuild-indexes` 通过，重建 2 个归档文件、2 条标准化记录和 2 条知识原子索引。

未完成项：

- 无 P1 阻断项。
- P2 仍需实现真实 AI 提炼、候选知识质量控制、隐私和敏感度判断提示词。

偏离 spec 的地方：

- 无。

下一步：

- P1 可以进入最终本地提交；提交后进入 P2 最小 AI 提炼实验。

## 2026-06-23 P1 待确认卡片闭环写入

执行目标：

- 阅读 `spec/项目评审报告.md` 复评第 7 节。
- 处理问题 12：`npm run p1:codex-import` 跑完后未写入 `knowledge/inbox/` 待确认 Markdown 卡片。

修改文件：

- `app/core/manual-import-normalization-runner.ts`
- `package.json`
- `spec/产品化开发步骤.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 在 `runManualImportNormalization` 中接入 `KnowledgeAtomMarkdownWriter`。
- 每条可进入个人知识库的 `NormalizedRecord` 会生成一条 P1 固定示例 `KnowledgeAtom`。
- 生成的知识原子状态为 `pending`，写入 `knowledge/inbox/`。
- 运行摘要新增 `generated_atom_count`，用于验收待确认卡片写入数量。
- 新增 `postinstall`，在依赖安装后自动执行 `npm rebuild better-sqlite3`，降低 Node ABI 不匹配导致首跑失败的风险。
- 已在当前环境执行 `npm rebuild better-sqlite3`。
- 已执行 `npm run p1:codex-import`，成功处理 2 个 Codex 示例文件，生成 2 条标准化记录和 2 张待确认卡片，失败 0。
- 保持 P1 边界：只生成固定示例待确认素材，不调用 AI，不做真实观点提炼。

未完成项：

- 尚未补最小测试套件。
- 尚未修正归档自我覆盖和 checksum 未持久化问题。

偏离 spec 的地方：

- 无。

下一步：

- 补最小测试锁住 P1 行为，并继续修正归档语义、checksum 持久化和隐私默认值。

## 2026-06-23 P1 无 UI 核心闭环入口

执行目标：

- 阅读最新 `spec/监督问题记录.md` 和 `spec/项目评审报告.md`。
- 处理 P1 验收拦截：补齐不打开桌面端也能完成 Codex 本地导入和标准化的核心闭环入口。

修改文件：

- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `app/core/core-engine.ts`
- `app/core/index.ts`
- `app/core/manual-import-normalization-runner.ts`
- `app/storage/local-raw-archive.ts`
- `scripts/run-manual-import-normalization.ts`
- `spec/产品化开发步骤.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 新增 `runManualImportNormalization`，串联目录确认、手动导入连接器、原始记录归档、标准化、SQLite 写入和本地运行日志。
- `LocalCoreEngine` 暴露 `runManualImportNormalization`，作为 Core 层可复用入口。
- 新增 `scripts/run-manual-import-normalization.ts`，只负责命令行参数解析并调用 Core 入口。
- 新增 `npm run p1:codex-import`，用于 P1 无 UI 验收。
- 将 `LocalRawArchive` 对齐 `StorageProvider.archiveRawDocument` 命名，同时保留旧 `archive` 方法。
- 已执行 `npm run p1:codex-import`，成功处理 2 个 Codex 示例文件，生成 2 条标准化记录，失败 0。
- 已通过 `npm run typecheck`。

未完成项：

- 尚未进入 P2 AI 提炼和质量控制。
- 项目评审报告中关于测试、归档语义、隐私默认值和路径工具复用的问题仍需后续逐项处理。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P2 前，优先处理项目评审报告中剩余的 P1/P2 风险，尤其是最小测试、归档语义和隐私默认值。

## 2026-06-22 P1 运行日志和错误记录

执行目标：

- 完成 `P1-15` 运行日志和错误记录。
- 日志只记录必要的运行事实，不写入大段原始对话、API Key 或本机绝对路径。

修改文件：

- `app/services/local-run-logger.ts`
- `app/services/index.ts`
- `spec/产品化开发步骤.md`
- `spec/执行日志.md`

完成项：

- 新增 `LocalRunLogger`，按日期写入本地 JSONL 运行日志。
- 支持 `info`、`warn`、`error` 三类日志入口。
- 日志字段限制为运行 ID、级别、事件类型、短消息、来源、文件名和记录数量。
- 原始路径只保留文件名，不记录本机绝对路径。
- 日志消息会截断到 240 字符，并对 `sk-` 风格 API Key 做脱敏。
- 已用临时 vault 写入 info 和 error 日志并验证脱敏结果。
- 已通过 `npm run typecheck`。

未完成项：

- 尚未进入 P2 AI 提炼和质量控制。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P2 前先做一次 P1 验收监督，确认核心闭环是否满足进入 AI 提炼阶段。

## 2026-06-22 P1 知识原子 Markdown 写入

执行目标：

- 继续 P1，按 `spec/知识原子Markdown模板.md` 实现固定示例 `KnowledgeAtom` 写入 Markdown 待确认区。
- 保持边界：只实现 Markdown 写入，不实现 AI 提炼和人工审核 UI。

修改文件：

- `package.json`
- `package-lock.json`
- `app/storage/knowledge-atom-markdown-writer.ts`
- `app/storage/index.ts`
- `spec/产品化开发步骤.md`
- `spec/执行日志.md`

完成项：

- 新增 `yaml` 依赖，用结构化方式生成 Markdown front matter。
- 新增 `KnowledgeAtomMarkdownWriter`，实现 `StorageProvider` 中 `writeKnowledgeAtom` 的子契约。
- 新增 `buildKnowledgeAtomMarkdownDocument`、`buildKnowledgeAtomPath` 和 `toSafeTitle`。
- 文件路径按 `knowledge/inbox/{created_date}-{atom_id}-{safe_title}.md` 生成。
- 已用固定示例写入临时 vault 并验证 front matter 和正文结构。
- 已通过 `npm run typecheck`。

未完成项：

- 尚未实现运行日志和错误记录。
- 尚未实现 AI 生成候选知识原子。

偏离 spec 的地方：

- 无。

下一步：

- 实现运行日志和错误记录。

## 2026-06-22 P1 SQLite 标准化记录存储

执行目标：

- 阅读最新 `spec/监督问题记录.md`。
- 完成 `P1-13` 标准化记录写入 SQLite。
- 明确 SQLite 存储实现与 `StorageProvider` 契约的关系。

修改文件：

- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `app/storage/sqlite-normalized-record-store.ts`
- `app/storage/index.ts`
- `spec/产品化开发步骤.md`
- `spec/任务拆分.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 新增 `better-sqlite3` 作为 SQLite 存储依赖，并提交锁文件变更。
- 新增 `SQLiteNormalizedRecordStore`，负责初始化 `normalized_records` 和 `schema_migrations` 表。
- 实现标准化记录批量 upsert 和按来源、项目、主题、时间范围查询。
- `SQLiteNormalizedRecordStore` 明确实现 `StorageProvider` 中标准化记录读写的子契约。
- 已用 Codex JSON 示例做 SQLite 写入和查询冒烟验证。
- 已通过 `npm run typecheck`。

未完成项：

- 尚未实现固定示例 `KnowledgeAtom` 写入 Markdown 待确认区。
- 尚未实现运行日志和错误记录。

偏离 spec 的地方：

- 无。

下一步：

- 实现固定示例 `KnowledgeAtom` 写入 Markdown 待确认区。

## 2026-06-22 P1 本地归档和手动导入解析

执行目标：

- 继续 P1，完成本地原始记录归档和 Markdown、TXT、JSON 手动导入解析。
- 保持边界：不实现 SQLite 写入、不写知识 Markdown、不调用 AI。

修改文件：

- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `app/connectors/source-connector.ts`
- `app/connectors/manual-import-connector.ts`
- `app/connectors/index.ts`
- `app/core/normalize-manual-import.ts`
- `app/core/index.ts`
- `app/storage/local-raw-archive.ts`
- `app/storage/workspace.ts`
- `app/storage/index.ts`
- `spec/产品化开发步骤.md`
- `spec/任务拆分.md`
- `spec/执行日志.md`

完成项：

- 新增 `ManualImportConnector`，支持从 vault-relative 导入目录识别 Markdown、TXT、JSON 文件。
- 新增 `LocalRawArchive`，支持将原始文档写入本地归档路径并生成内容校验值。
- 新增 `ensureWorkspaceDirectories`，运行时主动确认必要目录存在。
- 新增 `normalizeManualImport`，支持 JSON 简单单轮、JSON `messages[]` 多消息形态、Markdown front matter 和 TXT 标题段落解析。
- 保持 `raw_path` 为 vault-relative path，不把本机绝对路径写入标准化记录。
- 新增 `@types/node` 开发依赖并更新锁文件。
- 已通过 `npm run typecheck`。

未完成项：

- 尚未实现标准化记录写入 SQLite。
- 尚未实现固定示例 `KnowledgeAtom` 写入 Markdown 待确认区。
- 尚未实现运行日志和错误记录。

偏离 spec 的地方：

- 无。

下一步：

- 实现标准化记录写入 SQLite。

## 2026-06-22 P1 核心工程骨架和契约定义

执行目标：

- 阅读最新 `spec/监督问题记录.md`，确认没有新的待处理拦截项。
- 进入 P1，创建核心引擎相关最小 TypeScript 目录结构。
- 定义核心数据结构和接口契约，不提前实现导入、SQLite 或知识写入。

修改文件：

- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `app/index.ts`
- `app/core/core-engine.ts`
- `app/core/index.ts`
- `app/schemas/common.ts`
- `app/schemas/normalized-record.ts`
- `app/schemas/knowledge-atom.ts`
- `app/schemas/daily-run.ts`
- `app/schemas/migration.ts`
- `app/schemas/index.ts`
- `app/connectors/source-connector.ts`
- `app/connectors/index.ts`
- `app/storage/storage-provider.ts`
- `app/storage/index.ts`
- `app/services/credential-provider.ts`
- `app/services/index.ts`
- `app/workflow/.gitkeep`
- `spec/任务拆分.md`
- `spec/产品化开发步骤.md`
- `spec/执行日志.md`

完成项：

- 创建 P1 最小 `app/` 目录结构，不创建 `app/desktop/` 和 `build/`。
- 定义 `NormalizedRecord`、`KnowledgeAtom`、`DailyRun`、schema migration 入口。
- 定义 `SourceConnector`、`StorageProvider`、`CredentialProvider` 接口。
- `CredentialProvider` 只包含会话内凭据接口，不实现真实 API Key 持久化。
- 使用 npm 生成 `package-lock.json`，锁定 TypeScript 依赖版本。
- 新增 TypeScript 严格模式配置，并通过 `tsc --noEmit` 类型检查。

未完成项：

- 尚未实现本地原始记录归档。
- 尚未实现 Markdown、TXT、JSON 手动导入解析。
- 尚未实现 SQLite 写入。
- 尚未实现固定示例 `KnowledgeAtom` 写入 Markdown 待确认区。
- 尚未实现运行日志和错误记录。

偏离 spec 的地方：

- 无。

下一步：

- 实现本地原始记录归档和手动导入解析的最小闭环。

## 2026-06-18 项目目录英文改名

执行目标：

- 将项目目录从中文名改为英文名，方便后续创建远程仓库、CI 和跨平台工具使用。

修改文件：

- `spec/P0产品决策.md`
- `spec/任务拆分.md`
- `spec/产品化开发步骤.md`
- `spec/执行日志.md`

完成项：

- 确认英文仓库目录名为 `ai-chat-knowledge-system`。
- 将目录规划示例中的根目录同步为 `ai-chat-knowledge-system/`。
- 保留中文内部项目名和产品语义，不强行改成英文产品名。

未完成项：

- 尚未推送到远程仓库。

偏离 spec 的地方：

- 无。

下一步：

- 使用英文目录作为后续远程仓库名。

## 2026-06-18 P1 工程护栏复核修复

执行目标：

- 阅读 `spec/监督问题记录.md` 中最新 P1 工程护栏复核记录。
- 处理目录占位缺失和真实用户数据误提交风险。

修改文件：

- `.gitignore`
- `data/normalized/.gitkeep`
- `data/daily_runs/.gitkeep`
- `logs/.gitkeep`
- `knowledge/inbox/.gitkeep`
- `knowledge/approved/.gitkeep`
- `knowledge/rejected/.gitkeep`
- `knowledge/merged/.gitkeep`
- `prompts/.gitkeep`
- `scripts/.gitkeep`
- `raw/imports/cursor/.gitkeep`
- `raw/imports/deepseek/.gitkeep`
- `raw/imports/doubao/.gitkeep`
- `raw/imports/workbuddy/.gitkeep`
- `spec/任务拆分.md`
- `spec/产品化开发步骤.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 补齐 P1 需要版本化保留的目录占位文件。
- 新增 `knowledge/merged/` 目录占位。
- 默认忽略 `raw/imports/**` 和 `knowledge/**` 中的真实用户数据。
- 保留 `.gitkeep` 和已确认 Codex 示例文件的版本控制放行规则。
- 在规划文档中补充真实运行数据默认不提交到 git 的版本控制策略。

未完成项：

- 尚未进入 P1 Core Engine 代码实现。
- 尚未定义核心数据结构。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P1 最小目录结构和核心数据结构定义。

## 2026-06-18 P1 启动工程护栏

执行目标：

- 阅读 `spec/监督问题记录.md` 中最新 P1 启动监督记录。
- 按监督要求先补 P1 编码前工程护栏，不进入导入、SQLite 或知识写入实现。

修改文件：

- `.gitignore`
- `spec/知识原子Markdown模板.md`
- `spec/任务拆分.md`
- `spec/产品化开发步骤.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 新增 `.gitignore`，避免误提交依赖、构建产物、SQLite 本地数据库、日志、环境变量和临时文件。
- 设置仓库级 `git config core.quotepath false`，改善中文路径审查体验。
- 明确标准化记录以 SQLite 为主数据源，`data/normalized/` 只作为可选调试导出或人工检查。
- 新增 `KnowledgeAtom` Markdown 模板，覆盖文件命名、front matter、正文结构和状态规则。
- 收敛 P1 的 `CredentialProvider` 范围：只定义接口和类型，不实现真实 API Key 持久化。

未完成项：

- 尚未创建 P1 最小目录结构。
- 尚未定义核心数据结构。
- 尚未实现导入、SQLite 写入或知识写入代码。

偏离 spec 的地方：

- 无。

下一步：

- 创建 P1 核心引擎相关最小目录结构。
- 定义 `NormalizedRecord`、`KnowledgeAtom`、`DailyRun` 和相关接口。

## 2026-06-18 P1 放行前监督修复和基线提交

执行目标：

- 阅读 `spec/监督问题记录.md` 中最新 P1 放行前监督记录。
- 修复基线提交、旧监督覆盖说明、P0 确认来源、Electron 技术栈同步和 P1 目录范围问题。
- 建立 P0 规划基线提交。

修改文件：

- `spec/P0产品决策.md`
- `spec/产品化开发步骤.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`

完成项：

- 更正 P0 确认来源说明。
- 将主规划桌面壳同步为 Electron。
- 明确 P1 不创建 `build/`，只创建核心引擎相关最小目录。
- 在旧监督记录中补充历史判断覆盖说明。
- 已将 `spec/` 和 `raw/` 建立为 P0 baseline。

未完成项：

- 尚未进入 P1 编码。
- 尚未创建 P1 目录结构。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P1，创建核心引擎相关最小目录结构。

## 2026-06-18 P0 放行与 git 初始化

执行目标：

- 阅读 `spec/监督问题记录.md` 中最新 P0 执行监督记录。
- 按用户“阅读监督问题记录 开始干活”的指令同步 P0 确认状态。
- 处理 P0 状态、P0 任务勾选、AI 输入策略口径、Electron 技术栈确认、git 决策、`raw_path` 路径策略和 JSON 映射规则。

修改文件：

- `spec/P0产品决策.md`
- `spec/AI输入策略说明.md`
- `spec/产品化开发步骤.md`
- `spec/来源格式说明.md`
- `spec/任务拆分.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`
- `raw/imports/codex/示例记录.md`
- `raw/imports/codex/示例记录.json`

完成项：

- 将 P0 状态同步为已确认。
- 将 Electron 改为首版确认技术栈。
- 勾选 P0-01 到 P0-12。
- 统一 AI 输入策略口径：P0 确认原则和红线，P2 前确认实现细则。
- 明确 `raw_path` 使用 vault-relative path。
- 补充 JSON 简单单轮形态和 `messages[]` 多消息形态映射规则。
- 将正式编码前初始化 git 写入 P0 决策。
- 初始化项目目录 git 仓库。

未完成项：

- 尚未创建产品级 `app/` 和 `build/` 目录。
- 尚未进入 Core Engine 编码。

偏离 spec 的地方：

- 无。

下一步：

- 进入 P1，创建产品级目录结构。
- 定义核心数据结构。
- 实现 Codex 手动导入到标准化记录的最小闭环。

## 2026-06-18 处理规划调整复盘监督问题

执行目标：

- 阅读 `spec/监督问题记录.md` 中最新待处理问题。
- 按监督建议继续修 spec，不进入编码、不创建 `app/` 或 `build/`，不把 P0 标为已确认。

修改文件：

- `spec/P0产品决策.md`
- `spec/产品化开发步骤.md`
- `spec/任务拆分.md`
- `spec/来源格式说明.md`
- `spec/监督问题记录.md`
- `spec/执行日志.md`
- `spec/AI输入策略说明.md`
- `raw/imports/codex/示例记录.md`
- `raw/imports/codex/示例记录.json`

完成项：

- 明确当前只执行 P0，P0 未确认前不进入 P1。
- 保持 P0 为“草案，待用户确认，P0 未完成”。
- 将阶段 2 拆成 Codex MVP 子任务和 Cursor、DeepSeek P4 子任务。
- 将阶段 1 的“支持”改为“允许放入”，真正解析留到 P1。
- 新增 Codex JSON 示例记录。
- 统一 `raw_path` 和 `raw_source` 字段职责。
- 明确 `NormalizedRecord` 的单轮记录粒度。
- 明确 P1 只验证固定示例 `KnowledgeAtom` 写入待确认区。
- 前置 API Key 保存策略。
- 新增 AI 输入策略说明。
- 将 git 初始化作为 P0 确认后、正式编码前的门槛。

未完成项：

- P0 尚未由用户确认。
- 尚未初始化 git 仓库。
- 尚未进入正式编码。

偏离 spec 的地方：

- 无。

下一步：

- 请用户确认或修改 `spec/P0产品决策.md` 和 `spec/AI输入策略说明.md`。
- P0 确认后，再决定是否初始化 git 仓库。
- P0 确认后，再进入 P1 数据结构和导入标准化设计。

## 2026-06-17 处理监督问题记录

执行目标：

- 阅读 `spec/监督问题记录.md`。
- 处理其中关于规划优先级、P0 决策、存储边界、隐私前置、MVP 范围、来源格式、执行日志和示例输入的问题。

修改文件：

- `spec/P0产品决策.md`
- `spec/来源格式说明.md`
- `spec/执行日志.md`
- `spec/产品化开发步骤.md`
- `spec/任务拆分.md`
- `spec/监督问题记录.md`
- `raw/imports/codex/示例记录.md`

完成项：

- 新增 P0 决策承载文件。
- 新增来源格式说明。
- 新增执行日志。
- 新增 Codex 示例原始记录。
- 明确产品化规划优先级。
- 明确 P0 未确认前不创建 `app/` 和 `build/`。
- 明确 Markdown 与 SQLite 职责边界。
- 将最低限度隐私约束前置到 P0/P1/P2。
- 明确本地 MVP 只要求 Codex 一个来源跑通完整闭环，P4 再扩展三来源。
- 明确商业授权和自动更新在 MVP 只保留占位，不实现。

未完成项：

- 尚未进入正式编码。
- 尚未初始化 git 仓库。
- 尚未实现标准化脚本。

偏离 spec 的地方：

- 无。

下一步：

- 请用户确认 `spec/P0产品决策.md`。
- P0 确认后，再创建与技术栈匹配的最小产品级目录结构。
